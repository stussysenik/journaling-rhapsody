# feelcheck environment variables

# Admin authentication (bcrypt hash of your admin password)
# Generate with: node -e "require('bcryptjs').hash('your-password', 10).then(console.log)"
ADMIN_PASSWORD_HASH=

# Turso database
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
